/* ==========================================================================
   Custom Font Faces
   ========================================================================== */

/* Title Font */
@font-face {
  font-family: 'ARB';
  src: url('Fonts/Arb218BigBluntMar50Normal-q1Y2.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

/* Body Font (Replacing Bahiana) */
@font-face {
  font-family: 'OpsilonItalic';
  src: url('Fonts/OpsilonItalic-3lm9p.ttf') format('truetype');
  font-weight: normal;
  font-style: italic; 
  font-display: swap;
}

/* ==========================================================================
   Base Elements
   ========================================================================== */

html, body {
  margin: 0;
  padding: 0;
  width: 100%;
  background-color: #1a1a1a; /* Dark theme default */
  color: #f0f0f0;
}

/* Apply OpsilonItalic to all standard text */
body, p, span, a, li, button, input, textarea {
  font-family: 'OpsilonItalic', sans-serif;
  line-height: 1.6;
  font-size: 1rem;
}

/* ==========================================================================
   Typography
   ========================================================================== */

/* Apply ARB to all headings */
h1, h2, h3, h4, h5, h6, .title-text {
  font-family: 'ARB', sans-serif;
  font-weight: normal; 
  line-height: 1.2;
  margin-top: 0;
  margin-bottom: 1rem;
}

h1 {
  font-size: 3rem;
}

h2 {
  font-size: 2.5rem;
}

h3 {
  font-size: 2rem;
}

p {
  margin-top: 0;
  margin-bottom: 1.5rem;
}

a {
  color: #ff9900; 
  text-decoration: none;
  transition: color 0.3s ease;
}

a:hover {
  color: #ffcc00;
}

/* ==========================================================================
   Layout & Utility
   ========================================================================== */

.container {
  width: 90%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 2rem 0;
}

.text-center {
  text-align: center;
}

.highlight {
  color: #ff9900;
  font-family: 'ARB', sans-serif;
}